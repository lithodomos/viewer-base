import styled from "../style/styled";

export const UnstyledLink = styled.a`
  color: inherit;

  &:hover {
    text-decoration: none;
    color: inherit;
  }

  &:active {
    text-decoration: none;
    color: inherit;
  }
`;
