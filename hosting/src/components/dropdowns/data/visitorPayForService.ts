const options = [
  {
    value: 'Included in the ticket price',
    label: 'Included in the ticket price',
  },
  {
    value: 'On top of the ticket price (Optional)',
    label: 'On top of the ticket price (Optional)',
  },
];

export function getVisitorPayForServiceOptions() {
  return options;
}
